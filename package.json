{
  "scripts": {
    "test": "node --test",
    "claim": "node scripts/telecom-monthly-claim.js",
    "sms-server": "node scripts/sms-inbox-server.js",
    "lint": "bash -n scripts/*.sh && sh -n openwrt/* && python3 -m py_compile bwg/*.py && node --check scripts/telecom-monthly-claim.js && node --check scripts/sms-inbox-server.js && node --check scripts/home-http-proxy.js && node --check scripts/create-failure-issue.js && node --check scripts/resolve-run-log-outcome.js && node --check src/*.js",
    "home-proxy": "node scripts/home-http-proxy.js"
  },
  "dependencies": {
    "playwright": "^1.52.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20"
  },
  "name": "telecom-monthly-claim",
  "private": true
}
