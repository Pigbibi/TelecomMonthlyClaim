{
  "scripts": {
    "test": "node --test",
    "claim": "node scripts/telecom-monthly-claim.js",
    "sms-server": "node scripts/sms-inbox-server.js",
    "lint": "node --check scripts/telecom-monthly-claim.js && node --check scripts/sms-inbox-server.js && node --check scripts/create-failure-issue.js && node --check scripts/resolve-run-log-outcome.js && bash -n scripts/write-run-log-branch.sh && node --check src/*.js"
  },
  "dependencies": {
    "playwright": "^1.52.0",
    "playwright-extra": "^4.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  },
  "engines": {
    "node": ">=20"
  },
  "name": "telecom-monthly-claim",
  "private": true
}
